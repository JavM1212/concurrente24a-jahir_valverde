/**
 * @brief   Para no ser redundante, la documentacion individual de cada 
 *          funcion esta en el .c. Pero en general, el pseudo debe ser 
 *          mas lenguaje natural que un lenguaje conocido. Y se aprovecha a la 
 *          hora de programar como los planos del codigo
 */

procedure main
    create_thread(greet)
    print "Hello from main thread"
end procedure

procedure greet
    print "Hello from secondary thread"
end procedure